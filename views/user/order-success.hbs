 <section>
        <div class="container">
            <div class="row">
                <div class="col-md-12 order-placed-section">
                    <h1>Order placed successfully</h1>
                    <p>Your order has been received and is being processed.</p>
                    <a href="/orders" class="btn btn-primary">View Orders</a>
                </div>
            </div>
        </div>
    </section>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f2ff004c;
            margin: 0;
            padding: 0;
        }

        .container {
            padding: 20px;
        }

        .order-placed-section {
            text-align: center;
            margin: 100px auto;
            padding: 20px;
            border-radius: 10px;
            background-color: #fff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            max-width: 500px;
            animation: fadeInUp 0.8s ease;
        }

        .order-placed-section h1 {
            font-size: 28px;
            margin-bottom: 20px;
        }

        .order-placed-section .btn {
            font-size: 18px;
            padding: 10px 20px;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>