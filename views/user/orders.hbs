<div class="orders-container">
    <div class="container">
        <!-- Header -->
        <div class="orders-header">
            <div class="header-content">
                <h1 class="page-title">
                    <i class="fas fa-box"></i>
                    My Orders
                </h1>
                <p class="page-subtitle">Track and manage your orders</p>
            </div>
            <div class="header-actions">
                <a href="/" class="btn-continue-shopping">
                    <i class="fas fa-shopping-bag"></i>
                    Continue Shopping
                </a>
            </div>
        </div>

        {{#if orders.length}}
        <!-- Orders List -->
        <div class="orders-list">
            {{#each orders}}
            <div class="order-card">
                <div class="order-header">
                    <div class="order-info">
                        <div class="order-id">
                            <i class="fas fa-hashtag"></i>
                            <span>Order ID: {{this._id}}</span>
                        </div>
                        <div class="order-date">
                            <i class="fas fa-calendar-alt"></i>
                            <span>{{formatDate this.date}}</span>
                        </div>
                    </div>
                    <div class="order-status-badge status-{{this.status}}">
                        {{#if (eq this.status 'placed')}}
                            <i class="fas fa-check-circle"></i>
                            <span>Placed</span>
                        {{else if (eq this.status 'pending')}}
                            <i class="fas fa-clock"></i>
                            <span>Pending</span>
                        {{else if (eq this.status 'shipped')}}
                            <i class="fas fa-shipping-fast"></i>
                            <span>Shipped</span>
                        {{else if (eq this.status 'delivered')}}
                            <i class="fas fa-box-check"></i>
                            <span>Delivered</span>
                        {{else}}
                            <span>{{this.status}}</span>
                        {{/if}}
                    </div>
                </div>

                <div class="order-body">
                    <div class="order-details-grid">
                        <div class="detail-item">
                            <div class="detail-icon">
                                <i class="fas fa-map-marker-alt"></i>
                            </div>
                            <div class="detail-content">
                                <span class="detail-label">Delivery Address</span>
                                <span class="detail-value">{{this.deliveryDetails.address}}</span>
                            </div>
                        </div>

                        <div class="detail-item">
                            <div class="detail-icon">
                                <i class="fas fa-map-pin"></i>
                            </div>
                            <div class="detail-content">
                                <span class="detail-label">PIN Code</span>
                                <span class="detail-value">{{this.deliveryDetails.pincode}}</span>
                            </div>
                        </div>

                        <div class="detail-item">
                            <div class="detail-icon">
                                <i class="fas fa-phone"></i>
                            </div>
                            <div class="detail-content">
                                <span class="detail-label">Mobile</span>
                                <span class="detail-value">{{this.deliveryDetails.mobile}}</span>
                            </div>
                        </div>

                        <div class="detail-item">
                            <div class="detail-icon">
                                <i class="fas fa-credit-card"></i>
                            </div>
                            <div class="detail-content">
                                <span class="detail-label">Payment Method</span>
                                <span class="detail-value payment-badge">{{this.paymentMethod}}</span>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="order-footer">
                    <div class="order-total">
                        <span class="total-label">Total Amount:</span>
                        <span class="total-amount">₹{{this.totalAmount}}</span>
                    </div>
                    <a href="/view-order-products/{{this._id}}" class="btn-view-products">
                        <i class="fas fa-eye"></i>
                        View Products
                    </a>
                </div>
            </div>
            {{/each}}
        </div>
        {{else}}
        <!-- Empty State -->
        <div class="empty-state">
            <div class="empty-icon">
                <i class="fas fa-box-open"></i>
            </div>
            <h2>No Orders Yet</h2>
            <p>You haven't placed any orders yet. Start shopping to see your orders here!</p>
            <a href="/" class="btn-start-shopping">
                <i class="fas fa-shopping-bag"></i>
                Start Shopping
            </a>
        </div>
        {{/if}}
    </div>
</div>

<style>
    .orders-container {
        background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
        min-height: 100vh;
        padding: 40px 0;
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 20px;
    }

    /* Header */
    .orders-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 40px;
        flex-wrap: wrap;
        gap: 20px;
    }

    .header-content {
        flex: 1;
    }

    .page-title {
        font-size: 2.5rem;
        font-weight: 700;
        color: #2d3748;
        margin-bottom: 10px;
        display: flex;
        align-items: center;
        gap: 15px;
    }

    .page-title i {
        color: #4299e1;
    }

    .page-subtitle {
        font-size: 1.1rem;
        color: #718096;
        margin: 0;
    }

    .btn-continue-shopping {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        padding: 12px 24px;
        border-radius: 12px;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 10px;
        font-weight: 600;
        transition: all 0.3s ease;
        box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
    }

    .btn-continue-shopping:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(102, 126, 234, 0.4);
        color: white;
        text-decoration: none;
    }

    /* Orders List */
    .orders-list {
        display: flex;
        flex-direction: column;
        gap: 25px;
    }

    .order-card {
        background: white;
        border-radius: 16px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
        overflow: hidden;
        transition: all 0.3s ease;
    }

    .order-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
    }

    .order-header {
        background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
        padding: 20px 25px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        border-bottom: 1px solid #e2e8f0;
        flex-wrap: wrap;
        gap: 15px;
    }

    .order-info {
        display: flex;
        gap: 30px;
        flex-wrap: wrap;
    }

    .order-id, .order-date {
        display: flex;
        align-items: center;
        gap: 8px;
        color: #4a5568;
        font-weight: 500;
    }

    .order-id i, .order-date i {
        color: #4299e1;
    }

    .order-status-badge {
        padding: 8px 16px;
        border-radius: 20px;
        font-weight: 600;
        font-size: 0.875rem;
        display: flex;
        align-items: center;
        gap: 6px;
    }

    .order-status-badge.status-placed {
        background: #d4edda;
        color: #155724;
    }

    .order-status-badge.status-pending {
        background: #fff3cd;
        color: #856404;
    }

    .order-status-badge.status-shipped {
        background: #d1ecf1;
        color: #0c5460;
    }

    .order-status-badge.status-delivered {
        background: #d4edda;
        color: #155724;
    }

    .order-body {
        padding: 25px;
    }

    .order-details-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 20px;
    }

    .detail-item {
        display: flex;
        gap: 15px;
        align-items: flex-start;
    }

    .detail-icon {
        width: 40px;
        height: 40px;
        background: linear-gradient(135deg, #4299e1 0%, #3182ce 100%);
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 1rem;
        flex-shrink: 0;
    }

    .detail-content {
        display: flex;
        flex-direction: column;
        gap: 5px;
    }

    .detail-label {
        font-size: 0.875rem;
        color: #718096;
        font-weight: 500;
    }

    .detail-value {
        font-size: 1rem;
        color: #2d3748;
        font-weight: 600;
    }

    .payment-badge {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        padding: 4px 12px;
        border-radius: 15px;
        font-size: 0.875rem;
        display: inline-block;
        width: fit-content;
    }

    .order-footer {
        background: #f8fafc;
        padding: 20px 25px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        border-top: 1px solid #e2e8f0;
        flex-wrap: wrap;
        gap: 15px;
    }

    .order-total {
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .total-label {
        font-size: 1rem;
        color: #4a5568;
        font-weight: 500;
    }

    .total-amount {
        font-size: 1.5rem;
        font-weight: 700;
        color: #4299e1;
    }

    .btn-view-products {
        background: linear-gradient(135deg, #48bb78 0%, #38a169 100%);
        color: white;
        padding: 12px 24px;
        border-radius: 10px;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        font-weight: 600;
        transition: all 0.3s ease;
        box-shadow: 0 2px 8px rgba(72, 187, 120, 0.3);
    }

    .btn-view-products:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(72, 187, 120, 0.4);
        color: white;
        text-decoration: none;
    }

    /* Empty State */
    .empty-state {
        text-align: center;
        background: white;
        padding: 80px 40px;
        border-radius: 20px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
        max-width: 600px;
        margin: 0 auto;
    }

    .empty-icon {
        font-size: 5rem;
        color: #e2e8f0;
        margin-bottom: 30px;
    }

    .empty-state h2 {
        font-size: 2rem;
        font-weight: 700;
        color: #2d3748;
        margin-bottom: 15px;
    }

    .empty-state p {
        color: #718096;
        font-size: 1.1rem;
        line-height: 1.6;
        margin-bottom: 30px;
    }

    .btn-start-shopping {
        background: linear-gradient(135deg, #4299e1 0%, #3182ce 100%);
        color: white;
        padding: 15px 35px;
        border-radius: 12px;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 10px;
        font-weight: 600;
        font-size: 1.1rem;
        transition: all 0.3s ease;
        box-shadow: 0 4px 12px rgba(66, 153, 225, 0.3);
    }

    .btn-start-shopping:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(66, 153, 225, 0.4);
        color: white;
        text-decoration: none;
    }

    /* Mobile Responsiveness */
    @media (max-width: 768px) {
        .orders-container {
            padding: 20px 0;
        }

        .page-title {
            font-size: 2rem;
        }

        .orders-header {
            flex-direction: column;
            align-items: stretch;
        }

        .btn-continue-shopping {
            width: 100%;
            justify-content: center;
        }

        .order-header {
            flex-direction: column;
            align-items: stretch;
        }

        .order-info {
            flex-direction: column;
            gap: 10px;
        }

        .order-status-badge {
            align-self: flex-start;
        }

        .order-details-grid {
            grid-template-columns: 1fr;
        }

        .order-footer {
            flex-direction: column;
            align-items: stretch;
        }

        .btn-view-products {
            width: 100%;
            justify-content: center;
        }

        .empty-state {
            padding: 60px 30px;
        }

        .empty-state h2 {
            font-size: 1.5rem;
        }
    }
</style>